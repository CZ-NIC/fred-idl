module ccReg {

	interface Logger {
		TID new_event(in string sourceIP, in LogServiceType comp, in string content_in, in LogProperties props);
                boolean update_event(in TID id, in LogProperties props);
                boolean update_event_close(in TID id, in string content_out, in LogProperties props);
		TID new_session (in Languages lang, in string name, in string clTRID);	
		boolean end_session (in TID id, in string clTRID);
	};
};

